import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["192.168.29.51", "*.local"], // Specify allowed origins here
  /* other config options */
};

export default nextConfig;
